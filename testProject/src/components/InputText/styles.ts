import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  inputContainer: {
    width: "100%",
    minHeight: 40,
    paddingHorizontal: 8,
    paddingVertical: 4,
    marginVertical: 12,
    borderWidth: 1,
    borderRadius: 10,
    padding: 10,
  },
});
